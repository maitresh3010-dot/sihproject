# dataset/images

Sample package-label images for testing OCR and the scan pipeline
(JPG / JPEG / PNG / WEBP, max 10 MB each).

Naming: `<product>-<view>.jpg`, e.g. `abc-biscuits-front.jpg`.

Do not commit images containing personal data (addresses, phone numbers
of private individuals).
